FROM nginx:stable-alpine

# Nettoyage éventuel du contenu par défaut
RUN rm -rf /usr/share/nginx/html/*

# Copie du site
COPY . /usr/share/nginx/html

# Config nginx custom
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]