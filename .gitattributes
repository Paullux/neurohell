# NeuroHell — Git Attributes
# LFS complètement désactivé.
# Les gros assets (GLB, OGV, OGG…) sont gitignorés et servis par MinIO.
# Les images/textures embarquées dans le binaire Godot sont en git standard.
